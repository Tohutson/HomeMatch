spring:
  application:
    name: homematch

  profiles:
    default: dev

  config:
    import: optional:file:.env[.properties],optional:file:../.env[.properties]

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate